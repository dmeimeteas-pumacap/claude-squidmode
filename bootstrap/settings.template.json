{
  "//": "TEMPLATE — install.ps1 merges these keys into the recipient's ~/.claude/settings.json.",
  "//paths": "__CLAUDE_DIR__ is rewritten by install.ps1 to the recipient's actual ~/.claude path.",
  "//merge": "DECISION OPEN (README #2): merge-vs-prompt per key. These are the keys we touch.",

  "model": "opus[1m]",
  "effortLevel": "medium",
  "autoUpdatesChannel": "latest",
  "theme": "custom:cc-active",

  "statusLine": {
    "type": "command",
    "command": "powershell -NoProfile -ExecutionPolicy Bypass -File \"__CLAUDE_DIR__\\statusline.ps1\""
  },

  "//hooks": "Only present if the SessionStart/Stop hooks ship via bootstrap instead of the plugin (README #1). If the plugin carries them, omit this block entirely.",

  "//auto-update": "Declares the kit's OWN marketplace so a recipient auto-installs AND auto-updates it at startup. Third-party marketplaces are NOT auto-update by default, so a declarative entry with autoUpdate:true is the only way to make updates seamless. Merged add-if-absent (Merge-SettingsJson deep-merges these two object keys); never clobbers a recipient's existing marketplaces/plugins. The marketplace name and the plugin name are both 'dimitri-claude-kit', so the plugin ref is name@marketplace.",
  "extraKnownMarketplaces": {
    "dimitri-claude-kit": {
      "source": { "source": "github", "repo": "dmeimeteas-pumacap/claude-squidmode" },
      "autoUpdate": true
    }
  },
  "enabledPlugins": {
    "dimitri-claude-kit@dimitri-claude-kit": true
  },

  "//external-plugins": "The OPTIONAL enhancement plugins (andrej-karpathy-skills, code-simplifier) are still installed imperatively by install.ps1's Install-ExternalPlugins; they are not declared here."
}
